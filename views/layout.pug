- var pageTitle = 'Homepage'

block config
  - var userName = 'Tom'

doctype
html(lang="en")
  meta(charset="utf-8")
  head
    title #{pageTitle}

  body
    h1 #{pageTitle}
    block content
      include nav
